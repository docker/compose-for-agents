# Override compose file — use this when ANTHROPIC_API_KEY is set as an
# environment variable rather than in a secret file.
#
# Usage:
#   docker compose -f compose.yaml -f compose.env.yaml run jarvis

services:
  jarvis:
    secrets: []
